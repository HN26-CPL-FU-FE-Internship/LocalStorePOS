import AddNewButton from '@/components/common/AddNewButton';
import Search from '@/components/common/Search';

const HeaderReservation = () => {
    return (
        <>
            <Search />
            <AddNewButton name="circle-plus" onClick={() => {}} />
        </>
    );
};

export default HeaderReservation;
